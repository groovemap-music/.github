name: Validate shared Python policy
description: Enforce GrooveMap's bounded Python 3.14 package, tool, and managed-runtime policy.

runs:
  using: composite
  steps:
    - name: Validate pyproject policy
      shell: bash
      run: python3 "${{ github.action_path }}/validate.py" "${GITHUB_WORKSPACE}"
